services:
    # Extension sur les livres
    livre.extension:
          arguments: ["@livre.image"]
          class: BookBundle\Twig\LivreExtension
          tags:
              - { name: twig.extension }
