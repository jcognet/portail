{% import "macro/general.html.twig" as generalMacro %}
{% extends '@Commun/base.html.twig' %}
{% block javascripts %}
    <script src="{{ asset('js/liste.js') }}"></script>
    <script src="{{ asset('js/news_liste.js') }}"></script>
{% endblock %}

    {% block header %}
        Quoi de neuf ?
        {% if is_granted('ROLE_ADMIN') %}
            <small>
                <a href="{{ path('commun_news_enregistre') }}" title="Modifier une actualité">
                    <span class="glyphicon glyphicon-plus"></span>
                </a>
            </small>
        {% endif %}
    {% endblock %}
{% block body %}
    <div class="col-sm-6" id="liste_news_bloc">
        <div id="recherche_news_bloc">
            <h2 id="recherche_news_titre">
                Recherche de news
                <small>
                    <span id="recherche_livre_ouvrir" class="glyphicon glyphicon-resize-full"></span>
                    <span id="recherche_livre_fermer" class="glyphicon glyphicon-resize-small"></span>
                </small>
            </h2>
            <div id="recherche_news">
                {{ form(form_recherche) }}
            </div>
        </div>
        <div>
            <h2>Liste des actualités</h2>
            <div id="liste_news">
                {% embed 'CommunBundle:Block:news_liste.html.twig' %}
                {% endembed %}
            </div>
        </div>
    </div>
    <div class="col-sm-6">
        <div id="detail_news">

        </div>
    </div>
{% endblock %}

